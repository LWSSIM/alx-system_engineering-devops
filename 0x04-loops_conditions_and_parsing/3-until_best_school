#!/usr/bin/env bash
# Bash script to display "Best School" 10 times using an until loop

# Initialize a counter
counter=0

# Until loop to display "Best School" until the counter reaches 10
until [ $counter -ge 10 ]; do
    echo "Best School"
    
    # Increment the counter
    ((counter++))
done
